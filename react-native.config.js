/**
 * React Native project configuration.
 * Used by the React Native CLI for asset linking (fonts, images, etc.).
 *
 * Fonts are linked via: npx react-native-asset
 * Or automatically when running: react-native run-android / run-ios
 */

// eslint-disable-next-line no-undef
module.exports = {
  project: {
    ios: {},
    android: {},
  },
  assets: ['./src/assets/fonts/'],
};